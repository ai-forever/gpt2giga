# Inventory reservation fixture

This repository contains a deliberately small stock-reservation implementation.
It is used by the `gpt2giga-harness-p0-v1` comparative workflows and has no
runtime dependencies outside the Python standard library.

Run the seed tests with:

```bash
python -m unittest discover -s tests -v
```
