name: Publish to PyPI
on:
  release:
    types: [published]

jobs:
  pypi_release:
    name: Build with uv and publish to PyPI
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Set up uv
        uses: astral-sh/setup-uv@v5

      - name: Validate release tag matches package version
        shell: bash
        run: |
          set -euo pipefail
          TAG_VERSION="${GITHUB_REF_NAME#v}"
          PKG_VERSION="$(python -c 'import tomllib, pathlib; print(tomllib.loads(pathlib.Path("pyproject.toml").read_text("utf-8"))["project"]["version"])')"
          UI_PKG_VERSION="$(python -c 'import tomllib, pathlib; print(tomllib.loads(pathlib.Path("packages/gpt2giga-ui/pyproject.toml").read_text("utf-8"))["project"]["version"])')"
          echo "Release tag: ${GITHUB_REF_NAME} (version=${TAG_VERSION})"
          echo "pyproject.toml version: ${PKG_VERSION}"
          echo "packages/gpt2giga-ui/pyproject.toml version: ${UI_PKG_VERSION}"
          test "${TAG_VERSION}" = "${PKG_VERSION}"
          test "${TAG_VERSION}" = "${UI_PKG_VERSION}"

      - name: Build sdist and wheel
        shell: bash
        run: |
          set -euo pipefail
          rm -rf dist
          uv build
          (cd packages/gpt2giga-ui && uv build --out-dir ../../dist)

      - name: Publish to PyPI
        shell: bash
        env:
          PYPI_TOKEN: ${{ secrets.PYPI_API_KEY }}
        run: |
          set -euo pipefail
          uv publish --token "${PYPI_TOKEN}" dist/*
