# OpenAI Models API через `gpt2giga`

Эта папка содержит runnable-пример для получения списка моделей и запроса конкретной модели через Models API.

## Быстрый старт

1. Запустите прокси `gpt2giga`.
2. Запустите пример:

```bash
uv run python examples/openai/models/models.py
```
