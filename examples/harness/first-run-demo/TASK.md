# Inventory review task

Review the fictional inventory snapshot and identify items whose `on_hand`
quantity is at or below `reorder_at`.

For a later model-backed experiment, ask the agent to return a Markdown table
with the SKU, current quantity, reorder threshold, and a short recommendation.
Keep the first tour read-only and do not add credentials to this repository.
